- var p = __id
+nav

+hero(p.hero, "image")

+section
  +block
    p Entry Text
   
+section("images")
  +block
    +imageCollectionFromTo(p.images.collection, 0, p.images.collection.length - 1)

+section
  +projects-link("is-centred")